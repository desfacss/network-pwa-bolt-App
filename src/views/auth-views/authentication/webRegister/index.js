import React from "react";
import RegisterForm from "../../components/WebRegisterForm";
// import IBCNLayout from "components/layout-components/IBCNLayout";

const RegisterTwo = (props) => {
  return (
    <div>
      <RegisterForm {...props} />
    </div>
  );
};

export default RegisterTwo;
